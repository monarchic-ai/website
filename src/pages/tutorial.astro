---
import { webappBaseUrl } from "../lib/publicUrls";

const setupUrl = `${webappBaseUrl}/setup`;
// Astro's top-level redirect return is not counted as a read by its diagnostics.
void setupUrl;

return Astro.redirect(setupUrl, 301);
---
